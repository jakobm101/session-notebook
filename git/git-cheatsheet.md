# git repo setup
## git initialization
git add .
	gaa
git commit -m 'message'

## Connect to Github
gh create repo

# Git Cloning via SSH
git clone git:github.com/user/repo


# Git Branches
git switch
